# Independent QA — A5 startup cancellation, 2026-09-24

Reviewed commit: **1e09ec58f4f059c9d7f3e01fe875eb01b99ce588**. Baseline: `8d99b9334d529960781f749017d9258192b0c7c2`. Implementation: `04529d8`; profile documentation: `1e09ec5`.

**Decision: APPROVED.** A5 closes R3. Together with the accepted A1–A4 work and replacement evidence, the scoped local A2A MVP passes Planner QA; no material implementation blocker remains. This is the first execution of the constrained A5 successor; the predecessor's three executions and superseded disposition remain intact.

## Scope and source review

Only four committed files changed: `src/handoff_a2a/server.py`, `tests/test_a2a_lifecycle.py`, `tests/test_a2a_roundtrip.py`, and `docs/reference/a2a-coding-task-v1.md`. No changes to provider adapters, Planner orchestration, dependencies, process helpers, or the state-store implementation.

The launch thread now records the process handle, worker-start flag, workspace owner, and durable process identity under a spawn gate. Cancellation and safe lock release close the same gate, waiting for any in-flight launch to register. Cancellation before the gate prevents launch; cancellation during launch keeps the process owned so the existing stop/reap path can finish before release. The existing SQLite connection is configured for cross-thread use and its operations use a threading lock. `_run_validated` no longer relies on the canceled coroutine to retain the returned process handle.

This addresses the demonstrated failure directly. No additional blocking finding arose from the reviewed diff.

## Independent reproduction

Reran the same unmodified Planner script that previously reproduced R3 four times. Each disposable repository starts with app.py containing `value = 0` and a fake worker scheduled to write after three seconds. Submit through localhost A2A, immediately cancel without waiting for WORKER_STARTED, inspect processes, and observe beyond the scheduled write.

| Provider | Runs | Task after cancel | Lock after cancel | Live workers after cancel | Delayed write |
|---|---:|---|---|---|---|
| Fake Claude | 2 | CANCELED | Absent | None | None |
| Fake Codex | 2 | CANCELED | Absent | None | None |

All four record worker_launched=true and worker_stopped=true in partial-result evidence, keep app.py at `value = 0` and retain CANCELED after the observation period. The probe verified cleanup. Supplementary script: `/private/tmp/handoff-final-qa-20260924/probe_early_cancel.py`; results: `early-cancel-gl4dbbn7/results.json`. These temporary files are supplementary; the reproduction and outcome above are the durable evidence.

## Acceptance and regression checks

- Immediate cancellation: both fake providers now have a repository regression that checks delayed writes, process death, task state, lifecycle evidence, and commit count. The old roundtrip test now observes past the scheduled write.
- In-flight spawn: a deterministic barrier holds startup after process creation but before ownership registration. Cancellation remains pending with the workspace locked; after release, parent and child are dead before protection is removed. The same-workspace follow-up completes with no continuing child writes and exactly two total launches.
- Before spawn: a deterministic barrier lets cancellation close the gate first. Releasing the delayed launch cannot call start_owned; lifecycle records worker_started=false and worker_stopped=true.
- Existing completion, cancellation, deadlines, restart/reconnect, deduplication, round accounting, and R1/R2 tests are included in the full regression suite.
- Bash syntax: passed. Legacy smoke: **17 passed**. `git diff --check`: passed before final documentation edits.
- Python regression suite: **99 passed**, 100 SDK/protobuf deprecation warnings, 260.32 seconds. Ran `UV_CACHE_DIR=/private/tmp/handoff-qa-uv-cache uv run --offline --extra test python -m pytest -q` once against the reviewed commit.

## Decision boundary and remaining work

R1/R2 were accepted in the prior QA; their evidence and the independently verified Claude/Codex/mixed live fixtures remain valid. No new paid comparison is needed for this server ownership fix. This review does not claim better coding quality, speed, cost, or compatibility with an independently authored third-party server.

The selected-host skill discovery/status check remains Executor-reported; a full host-driven planning/approval/QA conversation and Cursor discovery remain untested, disclosed nonblocking limitations. An indefinitely hung operating-system spawn was not fault-injected. The new gate conservatively waits instead of releasing protection while startup is unresolved; broader fault injection is outside the agreed scope.

Planner made documentation-only updates and did not dispatch an Executor, make paid model calls, or modify runtime/tests. Human merge/push/adoption decisions remain separate.
